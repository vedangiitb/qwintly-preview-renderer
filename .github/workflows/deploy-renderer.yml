name: Deploy Renderer Service

on:
  push:
    branches:
      - main
      - dev
  workflow_dispatch:

env:
  SERVICE_NAME: qwintly-preview-renderer
  IMAGE_NAME: qwintly-preview-renderer

jobs:
  deploy:
    runs-on: ubuntu-latest

    environment:
      name: ${{ github.ref_name == 'main' && 'prod' || 'dev' }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Authenticate to GCP
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY_QWINTLY_GIT }}

      - name: Setup gcloud
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: '${{ vars.GCP_PROJECT_ID_QWINTLY }}'

      - name: Configure Docker
        run: |
          gcloud auth configure-docker ${{ vars.REGION }}-docker.pkg.dev

      - name: Build and Push Image
        run: |
          IMAGE=${{ vars.REGION }}-docker.pkg.dev/${{ vars.GCP_PROJECT_ID_QWINTLY }}/qwintly-images/${{ env.IMAGE_NAME }}:${{ github.sha }}

          docker build \
            --build-arg NEXT_PUBLIC_PARENT_ORIGIN=${{ vars.NEXT_PUBLIC_PARENT_ORIGIN }} \
            --build-arg LOCAL_PARENT_ORIGIN=${{ vars.LOCAL_PARENT_ORIGIN }} \
            --build-arg NODE_ENV=${{ vars.NODE_ENV }} \
            -t $IMAGE \
            .

          docker push $IMAGE

          echo "IMAGE=$IMAGE" >> $GITHUB_ENV

      - name: Deploy to Cloud Run
        run: |
          gcloud run deploy $SERVICE_NAME \
            --image $IMAGE \
            --region asia-south1 \
            --platform managed \
            --allow-unauthenticated \
            --service-account ${{ vars.PREVIEW_RENDERER_SA_EMAIL }} \
            --set-env-vars GCP_PROJECT_ID_QWINTLY=${{ vars.GCP_PROJECT_ID_QWINTLY }},NEXT_PUBLIC_SUPABASE_URL=${{ vars.NEXT_PUBLIC_SUPABASE_URL }},NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=${{ vars.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY }},NEXT_PUBLIC_PARENT_ORIGIN=${{ vars.NEXT_PUBLIC_PARENT_ORIGIN }},LOCAL_PARENT_ORIGIN=${{ vars.LOCAL_PARENT_ORIGIN }},NODE_ENV=${{ vars.NODE_ENV }} \
            --min-instances 0 \
            --max-instances 10 \
            --concurrency 1 \
            --timeout 3600 \
            --cpu 1 \
            --memory 1Gi
